import 'package:flutter/material.dart';

class TabReservas extends StatelessWidget {
  const TabReservas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Mis Libros Reservados"),
    );
  }
}
