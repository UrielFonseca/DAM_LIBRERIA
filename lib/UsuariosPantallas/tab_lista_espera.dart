import 'package:flutter/material.dart';

class TabListaEspera extends StatelessWidget {
  const TabListaEspera({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Lista de Espera (Pila LIFO)"),
    );
  }
}
