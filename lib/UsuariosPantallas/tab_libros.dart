import 'package:flutter/material.dart';

class TabLibros extends StatelessWidget {
  const TabLibros({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vista: Catálogo de Libros Disponibles"),
    );
  }
}
